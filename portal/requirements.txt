# SweGen PGx Portal — runtime dependencies. Pinned loosely; tighten if a
# release goes wonky. No safe-colab-cli dependency: the kernel manager
# in portal/kernel.py is self-contained.

# Web
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.5,<3.0
httpx>=0.27,<1.0

# Per-session Jupyter kernels
jupyter_client>=8.0
ipykernel>=6.29

# Hypha SDK (artifact-manager RPC + JWT helpers)
hypha-rpc>=0.20.0

# Scientific stack — pre-installed so the per-session kernels can
# `import pandas as pd` etc. without a first-call cold start.
pandas>=2.0
numpy>=1.26
matplotlib>=3.8
seaborn>=0.13
scipy>=1.12
